import React from 'react';

import { Grid } from './styles';

import Routes from '../../routes';

const Layout = () => {
  return (
    <Grid>
      <Routes />
    </Grid>
  );
};

export default Layout;
